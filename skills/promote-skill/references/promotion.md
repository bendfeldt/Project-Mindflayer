# Skill promotion mechanics

- Resolve both checkouts from repository context or explicit user input; do not hardcode personal paths.
- Promote the complete directory and preserve executable modes.
- Remove client-specific content and validate frontmatter plus `agents/openai.yaml`.
- Register each file in `manifest.tsv` with one shared skill lifecycle version.
- Preview changes before writing and never commit or publish automatically.

