interface:
  display_name: "Release Notes"
  short_description: "Plan release tasks and test notes"
  default_prompt: "Use $release-notes to plan child tasks and prepare testable release notes from a release pull request."
policy:
  allow_implicit_invocation: true
