# Promotion mechanics

- Resolve source and destination from current repository context or explicit user input.
- Remove client-specific identifiers and assumptions.
- Inspect collisions and dependencies before choosing a destination name.
- Preview the complete diff and request approval before writing.
- Update `manifest.tsv` for every distributable file.
- Validate after writing; never commit, push, or publish automatically.

