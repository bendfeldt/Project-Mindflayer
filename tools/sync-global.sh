#!/usr/bin/env bash
set -euo pipefail

printf 'Global synchronization is replacement-sensitive.\n'
printf 'Run install.sh --global --tools <explicit-list> --force to back up and refresh selected consumers.\n'
